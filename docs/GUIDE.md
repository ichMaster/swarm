# Swarm Evolution -- Детальний посiбник

Повний опис симулятора ройового iнтелекту з еволюцiйним вiдбором: як вiн працює, що можна змiнювати, та 10 навчальних сценарiїв для дослiдження емерджентної поведiнки.

---

## Змiст

1. [Що таке Swarm Evolution](#1-що-таке-swarm-evolution)
2. [Основнi поняття](#2-основнi-поняття)
3. [Як працює симуляцiя](#3-як-працює-симуляцiя)
4. [Геном та гени](#4-геном-та-гени)
5. [Еволюцiйний цикл](#5-еволюцiйний-цикл)
6. [Середовище](#6-середовище)
7. [Iнтерфейс користувача](#7-iнтерфейс-користувача)
8. [Параметри, якi можна змiнювати](#8-параметри-якi-можна-змiнювати)
9. [Система коментарiв](#9-система-коментарiв)
10. [Експорт та збереження](#10-експорт-та-збереження)
11. [10 навчальних сценарiїв](#11-10-навчальних-сценарiїв)
12. [Глосарiй](#12-глосарiй)

---

## 1. Що таке Swarm Evolution

Swarm Evolution -- це iнтерактивний симулятор, який моделює поведiнку рою автономних агентiв (боїдiв) у двовимiрному просторi. Кожен агент має власний набiр генiв, що визначають його поведiнку: як швидко вiн рухається, наскiльки тримається групи, як реагує на хижакiв та їжу.

Ключова особливiсть -- **еволюцiйний вiдбiр**: агенти, якi ефективно збирають їжу та уникають хижакiв, виживають довше, накопичують енергiю та розмножуються. Їхнi нащадки успадковують гени батькiв з невеликими мутацiями. Агенти, якi витрачають занадто багато енергiї або потрапляють до хижакiв, гинуть. З часом популяцiя адаптується до умов середовища -- це i є природний вiдбiр у дiї.

Симулятор побудований на класичнiй моделi **boids** (Крейга Рейнольдса, 1986), розширенiй еволюцiйною механiкою, енергетичною моделлю та AI-коментарями вiд Claude.

### Для кого цей симулятор

- **Студенти бiологiї** -- спостереження за природним вiдбором у реальному часi
- **Студенти iнформатики** -- вивчення емерджентної поведiнки та мультиагентних систем
- **Дослiдники AI** -- iнтуїцiя щодо ройового iнтелекту перед переходом до складнiших платформ
- **Викладачi** -- наочна демонстрацiя еволюцiйних концепцiй

---

## 2. Основнi поняття

### Боїд (Boid)

Автономний агент у симуляцiї. Термiн походить вiд "bird-oid" (птахоподiбний). Кожен боїд -- це точка на канвасi з позицiєю, швидкiстю, енергiєю, вiком та геномом з 7 генiв.

На екранi боїди вiдображаються як трикутники рiзних кольорiв. Колiр визначається геномом: вiдтiнок залежить вiд згуртованостi та вирiвнювання, насиченiсть -- вiд швидкостi, яскравiсть -- вiд сприйняття. Тому коли популяцiя конвергує (гени стають схожими), усi боїди набувають подiбного кольору -- це вiзуальний iндикатор еволюцiї.

### Хижак (Predator)

Червоний кружок, що полює на боїдiв. Хижак рухається до найближчого боїда в радiусi сприйняття (120 px) зi швидкiстю 1.8 одиниць. Якщо поруч немає здобичi, хижак блукає випадково. При контактi (вiдстань < 10 px) боїд гине миттєво.

### Їжа (Food)

Зелена крапка на канвасi. Коли боїд наближається до їжi на вiдстань менше 10 px, вiн з'їдає її та отримує +35 одиниць енергiї. Їжа одразу респавниться у випадковому мiсцi.

### Енергiя

Кожен боїд починає з 50 одиниць енергiї. Щотiку вiн витрачає енергiю за формулою:

```
витрата = 0.15 * (0.5 + швидкiсть * 0.5 + розмiр * 0.3)
```

Це означає, що швидкi та великi агенти витрачають бiльше енергiї -- це створює еволюцiйний тиск проти "суперагентiв", якi одночасно швидкi i великi.

### Тiк (Tick)

Один крок симуляцiї. За один тiк кожен боїд обчислює сили, оновлює позицiю, витрачає енергiю та перевiряє можливiсть розмноження. При швидкостi 1x за один кадр виконується 1 тiк; при 8x -- 8 тiкiв на кадр.

---

## 3. Як працює симуляцiя

### Крок симуляцiї (покроково)

Кожен тiк виконує такi дiї для кожного боїда:

**1. Пошук сусiдiв.** Боїд шукає iнших боїдiв у радiусi сприйняття. Радiус залежить вiд гену `perception`:

```
радiус = 30 + perception * 80 пiкселiв
```

Мiнiмальний радiус (при perception = 0.3) -- 54 px, максимальний (при 1.0) -- 110 px.

Для оптимiзацiї використовується **просторова сiтка** (spatial hash grid): канвас подiлений на комiрки 110x110 px, i пошук сусiдiв вiдбувається лише в 9 сусiднiх комiрках замiсть перевiрки всiх боїдiв.

**2. Обчислення сил Рейнольдса.** Для знайдених сусiдiв обчислюються три класичнi сили:

- **Вiдштовхування (Separation):** якщо сусiд ближче 20 px, боїд вiдштовхується вiд нього. Сила масштабується геном `separation` з коефiцiєнтом 1.5.
- **Вирiвнювання (Alignment):** боїд намагається рухатися в тому ж напрямку, що й сусiди. Сила масштабується геном `alignment` з коефiцiєнтом 0.4.
- **Згуртованiсть (Cohesion):** боїд рухається до центру маси сусiдiв. Сила масштабується геном `cohesion` з коефiцiєнтом 0.003.

**3. Втеча вiд хижакiв.** Якщо хижак знаходиться в радiусi сприйняття, боїд вiдштовхується вiд нього. Сила масштабується геном `fleeStrength` з коефiцiєнтом 3 -- це найсильнiша iз усiх сил, бо смерть вiд хижака миттєва.

**4. Пошук їжi.** Боїд рухається до найближчої їжi в радiусi сприйняття з фiксованою силою 0.5.

**5. Оновлення швидкостi.** Всi сили додаються до поточної швидкостi. Максимальна швидкiсть обмежена:

```
максимальна швидкiсть = 1.5 + speed * 2.5 одиниць/тiк
```

Якщо поточна швидкiсть перевищує максимум, вектор швидкостi нормалiзується.

**6. Оновлення позицiї.** Боїд рухається вiдповiдно до швидкостi. Краї канвасу обгортаються (тороїдальна топологiя): якщо боїд виходить за правий край, вiн з'являється злiва.

**7. Споживання їжi.** Якщо найближча їжа в межах 10 px, боїд її з'їдає (+35 енергiї), їжа респавниться.

**8. Витрата енергiї.** Формула: `0.15 * (0.5 + speed * 0.5 + size * 0.3)`.

**9. Старiння.** Вiк боїда збiльшується на 1.

**10. Розмноження.** Якщо енергiя > 70 i популяцiя не перевищує максимум (240), боїд розмножується:
- Батько втрачає половину енергiї
- Народжується нащадок поруч з батьком
- Геном нащадка -- мутована копiя генома батька

**11. Смерть.** Якщо енергiя <= 0, боїд гине i видаляється з симуляцiї.

### Поведiнка хижакiв

Пiсля обробки всiх боїдiв обробляються хижаки:
- Хижак шукає найближчого боїда в радiусi 120 px
- Якщо знайшов -- рухається до нього
- Якщо нi -- блукає випадково
- При контактi (< 10 px) боїд гине
- Кожен хижак може вбити лише одного боїда за тiк

### Пiдтримка популяцiї

Пiсля обробки всiх агентiв:
- Якщо популяцiя впала нижче 40, створюються новi випадковi боїди (аварiйний респавн)
- Кiлькiсть хижакiв та їжi синхронiзується з поточними параметрами слайдерiв

---

## 4. Геном та гени

Кожен боїд має геном з 7 генiв. Кожен ген -- це число вiд мiнiмуму до максимуму:

| Ген | Назва українською | Дiапазон | Що визначає |
|-----|-------------------|----------|-------------|
| `speed` | швидкiсть | 0.3 -- 1.0 | Максимальна швидкiсть руху. Вищi значення дозволяють швидше знаходити їжу та тiкати вiд хижакiв, але витрачають бiльше енергiї. |
| `perception` | сприйняття | 0.3 -- 1.0 | Радiус огляду (54-110 px). Бiльший радiус дозволяє бачити бiльше сусiдiв, їжу та хижакiв здалеку. |
| `cohesion` | згуртованiсть | 0.0 -- 1.0 | Наскiльки сильно боїд тягнеться до центру групи. Високi значення створюють щiльнi зграї. |
| `alignment` | вирiвнювання | 0.0 -- 1.0 | Наскiльки сильно боїд узгоджує напрямок руху з сусiдами. Високi значення створюють потоки. |
| `separation` | вiдштовхування | 0.3 -- 1.0 | Наскiльки сильно боїд уникає зiткнень з iншими. Запобiгає злиттю агентiв в одну точку. |
| `fleeStrength` | сила втечi | 0.2 -- 1.0 | Наскiльки сильно боїд тiкає вiд хижакiв. Низькi значення -- агент "хоробрий" (або нерозумний). |
| `size` | розмiр | 0.4 -- 1.0 | Вiзуальний розмiр трикутника. Бiльший розмiр витрачає бiльше енергiї. |

### Мутацiя

При розмноженнi кожен ген нащадка має шанс мутувати (визначається параметром "швидкiсть мутацiї"). Якщо мутацiя вiдбувається, значення гена змiнюється на випадкову величину в межах -0.15...+0.15, але не виходить за межi дiапазону гена.

Приклад: батько має `speed = 0.7`, мутацiя = +0.08. Нащадок отримає `speed = 0.78`.

### Кольорове кодування

Колiр боїда визначається його геномом:
- **Вiдтiнок** (hue) залежить вiд `cohesion` та `alignment` -- змiнюється вiд зеленуватого до синьо-фiолетового
- **Насиченiсть** залежить вiд `speed` -- швидшi агенти яскравiшi
- **Яскравiсть** залежить вiд `perception` -- агенти з бiльшим оглядом свiтлiшi

Коли популяцiя конвергує (всi гени стають подiбними), всi боїди набувають однакового кольору. Це вiзуальний сигнал, що еволюцiя знайшла стабiльну стратегiю.

---

## 5. Еволюцiйний цикл

### Тиск вiдбору

Еволюцiя вiдбувається через два механiзми природного вiдбору:

**Позитивний вiдбiр (розмноження):** агенти, якi ефективно збирають їжу i уникають хижакiв, накопичують енергiю вище 70 одиниць та розмножуються. Їхнi гени передаються нащадкам.

**Негативний вiдбiр (смерть):** агенти, якi витрачають забагато енергiї або потрапляють до хижакiв, гинуть. Їхнi гени зникають з генофонду.

### Компромiси (trade-offs)

Симуляцiя побудована на кiлькох ключових компромiсах:

1. **Швидкiсть vs. Енергiя:** швидшi агенти краще знаходять їжу i тiкають вiд хижакiв, але витрачають бiльше енергiї. В умовах дефiциту їжi повiльнi агенти можуть вижити довше.

2. **Розмiр vs. Енергiя:** бiльшi агенти витрачають бiльше енергiї без жодних переваг. Еволюцiя зазвичай вiдбирає маленьких агентiв -- якщо тiльки бiльший розмiр не дає побiчних переваг через випадковi кореляцiї.

3. **Згуртованiсть vs. Незалежнiсть:** зграї допомагають виявляти хижакiв (бiльше "очей"), але в умовах дефiциту їжi агенти в зграях конкурують за однi ресурси.

4. **Втеча vs. Годування:** якщо хижакiв мало, агенти з високою `fleeStrength` витрачають енергiю на непотрiбнi маневри. Але якщо хижакiв багато, низька `fleeStrength` -- це смертний вирок.

### Еволюцiйна динамiка

Типовий цикл еволюцiї:

1. **Початок (тiки 0-200):** випадкова популяцiя з рiзноманiтними геномами. Висока смертнiсть.
2. **Рання адаптацiя (тiки 200-500):** агенти з кращими генами починають домiнувати. Генетична рiзноманiтнiсть зменшується.
3. **Конвергенцiя (тiки 500-1000):** популяцiя стабiлiзується. Бiльшiсть генiв зближуються до оптимальних значень для поточних умов.
4. **Рiвновага (тiки 1000+):** стабiльний стан. Мутацiї створюють невелику варiацiю, але сильнi вiдхилення швидко вiдсiваються.

Якщо умови змiнюються (наприклад, користувач додає хижакiв), цикл починається знову -- популяцiя реадаптується.

---

## 6. Середовище

### Канвас

Середовище -- прямокутний простiр 1800 x 1200 пiкселiв з **тороїдальною топологiєю**: агент, що виходить за правий край, з'являється злiва; той, що виходить знизу, з'являється зверху. Це усуває "ефект стiни" -- агенти не скупчуються по краях.

### Сiтка

Темна сiтка з кроком 60 px видима на фонi канвасу. Вона не впливає на симуляцiю -- лише допомагає вiзуально оцiнювати вiдстанi та позицiї.

### Просторова оптимiзацiя

Для пошуку сусiдiв використовується рiвномiрна просторова сiтка з комiрками 110 px (максимальний радiус сприйняття). Канвас подiлений на 17 стовпцiв та 11 рядкiв = 187 комiрок. При пошуку сусiдiв перевiряються лише 9 сусiднiх комiрок (включаючи власну). Це дозволяє симулювати 1000+ боїдiв при 60 кадрах на секунду.

---

## 7. Iнтерфейс користувача

### Розташування елементiв

Iнтерфейс складається з трьох зон:

**Лiва частина (канвас):**
- Заголовок "Swarm Evolution -- Iteration 1"
- Канвас симуляцiї (1800x1200)
- Рядок кнопок: Pause, Reset, Seed, Export top 10, Експорт повного запуску, Export CSV, Export image, Нове вiкно
- Двi панелi коментарiв (жовта i зелена)
- Панель Claude (фiолетова, на всю ширину)

**Права частина (бiчна панель):**
- Слайдери управлiння (швидкiсть, їжа, хижаки, мутацiя)
- Метрики популяцiї (чисельнiсть, вiк, народження, смертi, з'їдено, тiк)
- Спарклайн популяцiї (графiк за останнi 200 тiкiв)
- Середнi значення генiв зi спарклайнами

### Клавiатурнi скорочення

| Клавiша | Дiя |
|---------|-----|
| `Space` | Пауза / продовження симуляцiї |
| `R` | Скидання симуляцiї (з дiалогом пiдтвердження) |
| `C` | Запитати Claude |
| `E` | Експорт топ-10 геномiв у консоль |
| `1`...`8` | Встановити швидкiсть симуляцiї |
| `?` | Показати/сховати пiдказку клавiш |

Скорочення не працюють, коли поле вводу (наприклад, Seed) має фокус.

### Адаптивний макет

При ширинi вiкна менше 1100 px iнтерфейс перебудовується: канвас розтягується на всю ширину (зберiгаючи пропорцiю 3:2), бiчна панель перемiщується пiд канвас, панелi коментарiв розташовуються вертикально.

---

## 8. Параметри, якi можна змiнювати

### Слайдери (змiнюються в реальному часi)

| Параметр | Слайдер | Дiапазон | За замовчуванням | Вплив |
|----------|---------|----------|------------------|-------|
| **Швидкiсть симуляцiї** | Sim speed | 1-8x | 1x | Кiлькiсть тiкiв за кадр. При 8x еволюцiя вiдбувається у 8 разiв швидше, але анiмацiя стає менш плавною. |
| **Кiлькiсть їжi** | Food | 5-200 | 80 | Бiльше їжi -- бiльше енергiї для популяцiї, швидше зростання, менший тиск вiдбору. |
| **Кiлькiсть хижакiв** | Predators | 0-6 | 2 | Бiльше хижакiв -- вищий тиск вiдбору на гени vteci та згуртованостi. |
| **Швидкiсть мутацiї** | Mutation rate | 0-50% | 15% | Ймовiрнiсть мутацiї кожного гена при розмноженнi. Висока мутацiя = бiльше рiзноманiтностi, але менше стабiльностi. |

### Поле Seed

Текстове поле поруч з кнопкою Reset. Якщо введено значення, симуляцiя використовує детермiнований генератор випадкових чисел (Mulberry32). Це означає, що однаковий seed завжди дає iдентичний результат -- корисно для вiдтворення експериментiв.

Якщо поле порожнє, використовується стандартний `Math.random()` (невiдтворюваний).

### Кнопки управлiння

| Кнопка | Дiя |
|--------|-----|
| **Pause / Resume** | Зупиняє або продовжує симуляцiю |
| **Reset** | Скидає симуляцiю: нова випадкова популяцiя, очищення iсторiї |
| **Export top 10** | Виводить 10 найстарших боїдiв у консоль браузера (F12) |
| **Експорт повного запуску** | Завантажує JSON-файл з повною iсторiєю |
| **Export CSV** | Завантажує CSV з тiками, популяцiєю та середнiми генами |
| **Export image** | Зберiгає поточний кадр канвасу як PNG |
| **Нове вiкно** | Вiдкриває нову вкладку з незалежною симуляцiєю |
| **Запитати Claude** | Надсилає поточний контекст до Claude для аналiзу |

---

## 9. Система коментарiв

Симулятор має три панелi коментарiв, що працюють незалежно:

### Панель 1 -- Дiї користувача (жовта)

Автоматично логує всi змiни параметрiв з позначкою тiку:

```
[t=342] Хижаки: 2 -> 5
[t=510] Їжа: 40 -> 20
[t=680] Мутацiя: 15% -> 30%
```

Зберiгає останнi 20 записiв. Це дозволяє вiдстежити, коли були зробленi якi змiни.

### Панель 2 -- Динамiка популяцiї (зелена)

Автоматичний детектор трендiв, що запускається кожнi 30 тiкiв. Порiвнює два 60-тiкованi вiкна (поточне та попереднє) i формує спостереження:

**Тренди популяцiї:**
- "Популяцiя зросла на 15 особин" -- якщо рiзниця > 8
- "Популяцiя стабiлiзувалася на ~65" -- якщо рiзниця < 3

**Тренди генiв:**
- "швидкiсть зросла на 0.08 (зараз 0.72)" -- якщо змiна > 0.05
- "згуртованiсть знизилась на 0.12 (зараз 0.45)"

**Каузальнi зв'язки (жовто-зеленi):** Пiсля змiни слайдера система чекає 60 тiкiв, потiм перевiряє, чи змiнилися гени. Якщо так, генерує реакцiю:

```
[реакцiя] Пiсля змiни хижакiв до 5, втеча зросла на 0.12 за 60 тiкiв
```

Це робить причинно-наслiдковий зв'язок мiж дiями користувача та еволюцiйною вiдповiддю явним.

### Панель 3 -- Коментар вiд Claude (фiолетова)

Натискання "Запитати Claude" надсилає поточний контекст (параметри, метрики, середнi гени, журнал дiй, тренди) до Claude через локальний proxy-сервер. Claude аналiзує ситуацiю i вiдповiдає трьома короткими абзацами:

1. **Оцiнка** -- до якого типу екологiчної нiшi конвергує популяцiя
2. **Прогноз** -- що станеться за найближчi 1-2 хвилини
3. **Рекомендацiя** -- що змiнити, щоб побачити цiкавий ефект

Пiд час запиту кнопка показує анiмований спiнер, а в панелi з'являються крапки, що рухаються. Вiдповiдь з'являється з плавною анiмацiєю.

Обмеження: 10 запитiв на хвилину на IP-адресу. Повторне натискання пiд час обробки iгнорується.

---

## 10. Експорт та збереження

### Автозбереження

Симуляцiя автоматично зберiгає стан кожнi 30 секунд та при закриттi вкладки. Зберiгаються: параметри слайдерiв, iсторiя популяцiї та генiв, журнал дiй, спостереження. Позицiї боїдiв НЕ зберiгаються -- при вiдновленнi створюється нова популяцiя з тими ж параметрами.

При перезавантаженнi, якщо збережений стан молодший за 24 години, з'являється банер з кнопками "Вiдновити" та "Почати нову".

### Багатовiконний режим

Кнопка "Нове вiкно" вiдкриває нову вкладку з незалежною симуляцiєю. Кожна вкладка отримує унiкальний iдентифiкатор i зберiгає стан в окремому слотi. Це дозволяє запускати два експерименти паралельно з рiзними параметрами.

### Формати експорту

| Формат | Кнопка | Що мiстить |
|--------|--------|------------|
| **JSON** | Експорт повного запуску | Повний стан: параметри, seed, iсторiя популяцiї, iсторiя генiв, журнал дiй, спостереження |
| **CSV** | Export CSV | Табличнi данi: тiк, популяцiя, середнi значення 7 генiв. Вiдкривається в Excel, Google Sheets, pandas. |
| **PNG** | Export image | Знiмок поточного кадру канвасу |
| **Console** | Export top 10 | 10 найстарiших боїдiв з геномами (виводиться в консоль браузера) |

---

## 11. 10 навчальних сценарiїв

### Сценарiй 1: Базова еволюцiя -- спостереження за конвергенцiєю

**Мета:** зрозумiти, як природний вiдбiр призводить до конвергенцiї генiв без будь-якого втручання.

**Налаштування:**
- Їжа: 40, Хижаки: 2, Мутацiя: 15%, Швидкiсть: 4x
- Seed: 42 (для вiдтворюваностi)

**Що робити:**
1. Запустiть симуляцiю та спостерiгайте 3-5 хвилин
2. Слiдкуйте за спарклайнами генiв у бiчнiй панелi
3. Зверніть увагу на змiну кольорiв боїдiв

**Що спостерiгати:**
- На початку боїди рiзних кольорiв -- це вiзуальне вiдображення генетичної рiзноманiтностi
- Через 1-2 хвилини кольори починають зближуватись -- популяцiя конвергує
- Спарклайни генiв стабiлiзуються на певних значеннях
- Панель 2 повiдомляє: "Популяцiя стабiлiзувалася на ~N"

**Питання для обговорення:**
- Чому не всi гени конвергують з однаковою швидкiстю?
- Який ген конвергує першим i чому?
- Що буде, якщо запустити з iншим seed?

---

### Сценарiй 2: Тиск хижакiв -- еволюцiя оборонних стратегiй

**Мета:** дослiдити, як збiльшення загрози хижакiв змiнює еволюцiйну стратегiю популяцiї.

**Налаштування:**
- Почати з: Їжа: 40, Хижаки: 0, Мутацiя: 15%, Швидкiсть: 4x

**Що робити:**
1. Запустiть з 0 хижакiв на 2 хвилини -- це базова лiнiя
2. Запишiть середнi значення `cohesion`, `fleeStrength`, `speed`
3. Збiльшiть хижакiв до 4
4. Спостерiгайте 3 хвилини
5. Збiльшiть хижакiв до 6
6. Спостерiгайте ще 3 хвилини

**Що спостерiгати:**
- При 0 хижакiв: низька `cohesion` та `fleeStrength` (немає потреби)
- При 4 хижакiв: стрiмке зростання `fleeStrength` та `cohesion`
- При 6 хижакiв: можливий обвал популяцiї, потiм вiдновлення з ще вищими оборонними генами
- Каузальнi зв'язки у панелi 2 явно покажуть реакцiю на змiни

**Питання для обговорення:**
- Чому `cohesion` зростає при появi хижакiв? (пiдказка: зграя -- це захист)
- Чи є максимальний рiвень хижакiв, при якому популяцiя не може вижити?
- Як змiнюється `speed` -- зростає чи падає?

---

### Сценарiй 3: Дефiцит ресурсiв -- боротьба за виживання

**Мета:** дослiдити, як обмеженiсть ресурсiв впливає на еволюцiйнi стратегiї.

**Налаштування:**
- Їжа: 5, Хижаки: 1, Мутацiя: 15%, Швидкiсть: 4x

**Що робити:**
1. Запустiть з мiнiмумом їжi та спостерiгайте 3 хвилини
2. Зверніть увагу на коливання популяцiї
3. Натиснiть "Запитати Claude" для аналiзу ситуацiї
4. Поступово збiльшуйте їжу до 20, потiм до 60, потiм до 100
5. На кожному етапi зачекайте 2 хвилини та запишiть значення генiв

**Що спостерiгати:**
- При дефiцитi їжi популяцiя коливається бiля мiнiмуму (40)
- Еволюцiя вiдбирає агентiв з низькою `speed` та `size` (енергоефективних)
- При надлишку їжi (100) -- `cohesion` може падати (немає потреби в зграях для пошуку)
- Рiзнi рiвнi ресурсiв вiдбирають рiзнi стратегiї

**Питання для обговорення:**
- Чому при дефiцитi їжi повiльнi агенти виживають краще за швидких?
- Як змiнюється `perception` при рiзних рiвнях ресурсiв?
- Порiвняйте з реальними екосистемами: чому на бiдних грунтах рослини ростуть повiльно?

---

### Сценарiй 4: Мутацiйний стрес-тест

**Мета:** зрозумiти роль швидкостi мутацiї в еволюцiї.

**Налаштування:**
- Їжа: 30, Хижаки: 2, Швидкiсть: 4x

**Що робити:**
1. Запустiть з мутацiєю 0% на 2 хвилини -- зафiксуйте стан генiв
2. Встановiть мутацiю 5% -- зачекайте 3 хвилини
3. Встановiть мутацiю 15% -- зачекайте 3 хвилини
4. Встановiть мутацiю 50% -- зачекайте 3 хвилини
5. Порiвняйте стабiльнiсть генiв на кожному етапi

**Що спостерiгати:**
- При 0%: нульова еволюцiя -- першi випадковi гени залишаються назавжди
- При 5%: повiльна, стабiльна адаптацiя -- спарклайни плавно рухаються до оптимуму
- При 15%: здорова еволюцiя -- швидка адаптацiя зi стабiльним результатом
- При 50%: хаос -- спарклайни стрибають, популяцiя нестабiльна, еволюцiя не може "зафiксувати" хорошi гени

**Питання для обговорення:**
- Чому надто висока мутацiя шкiдлива? (пiдказка: "мутацiйне навантаження")
- Чому надто низька мутацiя теж проблема? (пiдказка: вiдсутнiсть адаптацiї)
- Яка оптимальна швидкiсть мутацiї i чи залежить вона вiд умов?

---

### Сценарiй 5: Вiдтворюванiсть -- наука потребує повторення

**Мета:** зрозумiти значення детермiнiзму та вiдтворюваностi в наукових експериментах.

**Налаштування:**
- Їжа: 40, Хижаки: 3, Мутацiя: 15%, Швидкiсть: 4x, Seed: "experiment-1"

**Що робити:**
1. Введiть seed "experiment-1" i натиснiть Reset
2. Запустiть симуляцiю на 2 хвилини, запишiть кiнцевi значення генiв
3. Натиснiть Reset (seed залишається)
4. Запустiть знову на 2 хвилини -- порiвняйте результати
5. Змiнiть seed на "experiment-2" -- порiвняйте з попереднiм

**Що спостерiгати:**
- Обидва запуски з seed "experiment-1" дають абсолютно iдентичнi результати -- однаковi позицiї, однаковi генетичнi тренди
- Seed "experiment-2" дає iнший, але теж вiдтворюваний результат
- Без seed кожен запуск рiзний

**Питання для обговорення:**
- Чому вiдтворюванiсть важлива в науцi?
- Якщо два рiзних seed дають рiзнi результати, як визначити, який результат "правильний"?
- Скiльки запускiв потрiбно для статистично значущих висновкiв?

---

### Сценарiй 6: Порiвняльний аналiз -- два вiкна, двi стратегiї

**Мета:** безпосередньо порiвняти двi рiзнi еволюцiйнi стратегiї.

**Налаштування:**
- Вiкно 1: Їжа: 60, Хижаки: 0, Мутацiя: 15%, Seed: "compare-1"
- Вiкно 2: Їжа: 60, Хижаки: 6, Мутацiя: 15%, Seed: "compare-1"

**Що робити:**
1. Налаштуйте першу вкладку як зазначено
2. Натиснiть "Нове вiкно" -- вiдкриється нова вкладка
3. У новiй вкладцi встановiть 6 хижакiв (решту залишiть)
4. Запустiть обидвi на 5 хвилин
5. Зробiть Export CSV в обох вкладках
6. Порiвняйте данi

**Що спостерiгати:**
- Однаковий seed, але рiзне середовище -- рiзнi еволюцiйнi шляхи
- Без хижакiв: "розслабленi" гени -- низька `fleeStrength`, низька `cohesion`
- З хижаками: "вiйськовi" гени -- висока `fleeStrength`, висока `cohesion`
- `speed` може бути подiбною в обох -- швидкiсть корисна завжди

**Питання для обговорення:**
- Як тиск середовища формує генотип?
- Порiвняйте з реальними прикладами: острiвнi тварини vs. материковi
- Чому деякi гени конвергують однаково в обох сценарiях?

---

### Сценарiй 7: Катастрофа та вiдновлення

**Мета:** дослiдити, як популяцiя вiдновлюється пiсля раптової катастрофи.

**Налаштування:**
- Почати з: Їжа: 50, Хижаки: 2, Мутацiя: 15%, Швидкiсть: 4x

**Що робити:**
1. Запустiть i дочекайтеся стабiлiзацiї (3-4 хвилини)
2. Запишiть стабiльнi значення генiв та чисельнiсть
3. КАТАСТРОФА: одночасно встановiть Їжу = 5, Хижакiв = 6
4. Спостерiгайте обвал популяцiї
5. Через 1 хвилину поверніть нормальнi умови: Їжа = 50, Хижаки = 2
6. Спостерiгайте вiдновлення

**Що спостерiгати:**
- Обвал популяцiї до мiнiмуму (20, аварiйний респавн)
- Пiсля вiдновлення умов -- швидке зростання
- АЛЕ: пiсля "пляшкового горлечка" генетична рiзноманiтнiсть знижена
- Гени пiсля катастрофи можуть вiдрiзнятися вiд попередньо стабiльних

**Питання для обговорення:**
- Що таке "ефект пляшкового горлечка" (bottleneck effect)?
- Чому пiсля катастрофи популяцiя може мати iншi гени, нiж до неї?
- Порiвняйте з реальними прикладами: пiвнiчний морський слон, гепард

---

### Сценарiй 8: Рай без тиску -- деградацiя чи стабiльнiсть?

**Мета:** дослiдити, що вiдбувається з еволюцiєю, коли вiдсутнiй тиск вiдбору.

**Налаштування:**
- Їжа: 100, Хижаки: 0, Мутацiя: 15%, Швидкiсть: 4x

**Що робити:**
1. Запустiть "райськi" умови на 5 хвилин
2. Запишiть значення генiв
3. Натиснiть "Запитати Claude" для аналiзу
4. Порiвняйте з результатами сценарiю 2 (високий тиск хижакiв)

**Що спостерiгати:**
- Популяцiя швидко досягає максимуму (240)
- `cohesion` та `fleeStrength` знижуються -- немає потреби
- `speed` може знижуватись -- менше потреби шукати їжу
- `size` -- цiкавий випадок: чи вiдбирається малий розмiр (менша витрата)?

**Питання для обговорення:**
- Чому в "раю" еволюцiя все одно вiдбувається?
- Що таке "генетичний дрейф"?
- Порiвняйте з реальними прикладами: печернi тварини, що втрачають зiр

---

### Сценарiй 9: Еволюцiйна гонка озброєнь

**Мета:** моделювати поступову ескалацiю тиску та спостерiгати за адаптацiєю.

**Налаштування:**
- Почати з: Їжа: 40, Хижаки: 0, Мутацiя: 15%, Швидкiсть: 4x

**Що робити:**
1. Запустiть з 0 хижакiв -- зачекайте 2 хвилини
2. Додайте 1 хижака -- зачекайте 2 хвилини
3. Додайте ще 1 (стало 2) -- зачекайте 2 хвилини
4. Продовжуйте додавати по 1 кожнi 2 хвилини до 6
5. Пiсля кожного кроку натискайте "Запитати Claude"

**Що спостерiгати:**
- Поступове зростання `fleeStrength` при кожному додаваннi хижака
- `cohesion` зростає -- зграї як захист
- Популяцiя може залишатись стабiльною при поступовому додаваннi, але обвалитись при рiзкому
- Claude коментатиме кожен етап i давати прогнози

**Питання для обговорення:**
- Чому поступова ескалацiя дозволяє адаптуватися, а рiзка -- нi?
- Порiвняйте з концепцiєю "вареної жаби" в екологiї
- Що таке "еволюцiйна гонка озброєнь" (Red Queen hypothesis)?

---

### Сценарiй 10: Збiр та аналiз даних -- кiлькiсний пiдхiд

**Мета:** навчитися збирати данi, аналiзувати їх та робити висновки на основi кiлькiсних показникiв.

**Налаштування:**
- 4 окремих запуски з seed "data-1", "data-2", "data-3", "data-4"
- Для кожного: Їжа: 40, Хижаки: 3, Мутацiя: 15%, Швидкiсть: 8x

**Що робити:**
1. Для кожного seed:
   a. Встановiть seed, натиснiть Reset
   b. Запустiть на 3 хвилини (при 8x це ~1440 тiкiв)
   c. Натиснiть "Export CSV"
2. Вiдкрийте всi 4 CSV-файли в Excel або Google Sheets
3. Побудуйте графiки: середнє значення кожного гена по тiках для всiх 4 запускiв
4. Обчислiть середнє та стандартне вiдхилення кiнцевих значень генiв

**Що спостерiгати:**
- Всi 4 запуски конвергують до подiбних, але не iдентичних значень
- Деякi гени мають малу варiацiю (сильний тиск вiдбору), iншi -- велику (слабкий тиск)
- Можна обчислити "стабiльнiсть" кожного гена як 1/стандартне_вiдхилення

**Що аналiзувати:**
- Який ген найбiльш стабiльний (найменша варiацiя мiж запусками)?
- Який ген найменш стабiльний i чому?
- Чи можна передбачити кiнцевi значення генiв, знаючи параметри середовища?

**Питання для обговорення:**
- Скiльки запускiв потрiбно для надiйних висновкiв?
- Як визначити, чи статистично значуща рiзниця мiж двома умовами?
- Як цей пiдхiд вiдрiзняється вiд "подивився один раз i зробив висновок"?

---

## 12. Глосарiй

| Термiн | Визначення |
|--------|-----------|
| **Боїд (Boid)** | Автономний агент у симуляцiї, що рухається за правилами Рейнольдса та має еволюцiйний геном |
| **Геном** | Набiр з 7 генiв, що визначають поведiнку боїда |
| **Ген** | Числове значення в дiапазонi [мiн, макс], що контролює один аспект поведiнки |
| **Мутацiя** | Випадкова змiна значення гена при розмноженнi |
| **Тiк** | Один крок симуляцiї |
| **Конвергенцiя** | Процес зближення значень генiв у популяцiї до оптимальних |
| **Дрейф** | Випадковi змiни частоти генiв, не пов'язанi з тиском вiдбору |
| **Пляшкове горлечко** | Рiзке зменшення популяцiї, що знижує генетичну рiзноманiтнiсть |
| **Тиск вiдбору** | Фактори середовища, що визначають, якi гени дають перевагу |
| **Емерджентна поведiнка** | Складна колективна поведiнка, що виникає з простих iндивiдуальних правил |
| **Правила Рейнольдса** | Три правила руху боїдiв: вiдштовхування, вирiвнювання, згуртованiсть |
| **Тороїдальна топологiя** | Простiр, де протилежнi краї з'єднанi (вихiд справа = вхiд злiва) |
| **Просторова сiтка** | Структура даних для швидкого пошуку сусiдiв у просторi |
| **Детермiнований ГПВЧ** | Генератор псевдовипадкових чисел, що дає iдентичну послiдовнiсть при однаковому seed |
| **Спарклайн** | Мiнiатюрний графiк, що показує тренд значення за останнi 200 тiкiв |
| **Каузальний зв'язок** | Причинно-наслiдкова залежнiсть мiж дiєю користувача та реакцiєю популяцiї |
| **Компромiс (trade-off)** | Ситуацiя, коли покращення одного параметра погiршує iнший |
| **Фiтнес** | Здатнiсть агента вижити та розмножитися в поточних умовах |
