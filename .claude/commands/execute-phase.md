Execute phase $ARGUMENTS of the project roadmap using the /execute-phase skill. Follow the full pipeline: discover issues from GitHub labeled phase-$ARGUMENTS, build a dependency-sorted execution plan, then implement each issue one by one — generate code, validate against every acceptance criterion in specification/ISSUES.md, run tests, fix failures, commit, push, and close the GitHub issue. After all issues are done, run the full test suite and output a summary report.
